{
  "name": "jeview",
  "version": "0.1.0",
  "private": true,
  "description": "A local middleman for Jev (TypeSafe) with a live view of every call.",
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "start": "node jeview.ts",
    "demo": "node scripts/demo.ts",
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
